<?xml version="1.0" encoding="EUC-KR"?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.emp-mapper">
	<select id="empAllData" resultType="Emp">
		SELECT empno,ename,job,hiredate,sal FROM emp
	</select>
	<select id="empFindData" resultType="Emp" parameterType="int">
	 SELECT empno,ename,job,hiredate,sal FROM emp WHERE empno=#{empno}
	<!-- 
		String data=map.get("empFindData");
		data.replace('#',?);
		data.subString(0,data.lastIndexOf('?')+1);
	
	 -->
	</select>
	<!--  
	<select id="empFindData1" resultType="Emp" parameterType="Emp">
	 resultType에서 EmpDTO라고 데이터 형태를 받아 출력한다. 
	 SELECT empno,ename,job,hiredate,sal FROM emp WHERE empno BETWEEN #{job} AND #{sal}
	 
	
		String data=map.get("empFindData");
		data.replace('#',?);
		data.subString(0,data.lastIndexOf('?')+1);
	
	 
	</select>
	<select id="empFindData1" resultType="Emp" parameterType="Map">
	 SELECT empno,ename,job,hiredate,sal FROM emp WHERE empno BETWEEN #{start} AND #{end}
	 
		String data=map.get("empFindData");
		data.replace('#',?);
		data.subString(0,data.lastIndexOf('?')+1);
	
	
	 
	</select>
	 -->
</mapper>